{
  "name": "mypail",
  "version": "0.2.0-beta",
  "description": "Emotional AI Agent - Clean Build",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "jest --verbose",
    "test:watch": "jest --watch",
    "build:web": "mkdir -p dist && cp docs/client.html dist/index.html && rm -rf dist/avatars && cp -r avatars dist/avatars && npx esbuild src/capacitor-bridge.js --bundle --outfile=dist/capacitor-bridge.js --format=iife",
    "cap:sync": "npm run build:web && npx cap sync",
    "cap:open": "npx cap open android",
    "build:apk": "npm run cap:sync && JAVA_HOME=/home/stjun12/jdk-21.0.6+7 ANDROID_HOME=/home/stjun12/Android/Sdk ./android/gradlew -p android assembleDebug && rclone copy android/app/build/outputs/apk/debug/app-debug.apk gdrive:MyPAiL/ && echo '✅ APK uploaded to Google Drive > MyPAiL'"
  },
  "keywords": [
    "ai",
    "emotion",
    "anthropic",
    "claude"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@anthropic-ai/sdk": "^0.71.2",
    "@capacitor-community/speech-recognition": "^7.0.1",
    "@capacitor-community/text-to-speech": "^8.0.0",
    "@capacitor/android": "^8.0.2",
    "@capacitor/cli": "^8.0.2",
    "@capacitor/core": "^8.0.2",
    "@capacitor/device": "^8.0.0",
    "@capacitor/network": "^8.0.0",
    "cors": "^2.8.5",
    "dotenv": "^17.2.4",
    "express": "^4.22.1",
    "groq-sdk": "^0.37.0",
    "node-fetch": "^2.7.0"
  },
  "devDependencies": {
    "esbuild": "^0.27.3",
    "jest": "^29.7.0",
    "nodemon": "^3.0.1"
  }
}
